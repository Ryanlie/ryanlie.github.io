{
	"name": "ryanlie.github.io",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"serve": "pnpm browser-sync --server -f page/ --no-open --no-notify --no-ui --extensions html"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"browser-sync": "^2.28.1",
		"concurrently": "^7.6.0",
		"sass": "^1.58.3"
	}
}
